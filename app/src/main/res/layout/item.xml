<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <CheckBox
        android:id="@+id/like_tab"
        style="@style/Custom.LikeRadioButton"
        android:layout_width="70dp"
        android:layout_height="70dp"

        android:drawableTop="@drawable/tab_like_selector"
        app:layout_constraintBottom_toBottomOf="@id/text_info"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/text_title" />

    <TextView

        android:id="@+id/text_title"
        android:layout_width="330dp"
        android:layout_height="wrap_content"
        android:maxLines="99"
        android:text="标题将会显示在这里"
        android:textColor="#000002"
        android:textSize="20sp"
        android:textStyle="bold"

        app:layout_constraintEnd_toEndOf="parent"

        app:layout_constraintLeft_toRightOf="@id/like_tab"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/text_info"
        android:layout_width="333dp"
        android:layout_height="27dp"
        android:gravity="center_vertical"
        android:text="标签将会显示在这里"
        android:textSize="15sp"
        app:layout_constraintEnd_toEndOf="parent"

        app:layout_constraintStart_toEndOf="@+id/like_tab"
        app:layout_constraintTop_toBottomOf="@+id/text_title" />


</androidx.constraintlayout.widget.ConstraintLayout>